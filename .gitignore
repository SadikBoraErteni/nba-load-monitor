.venv/
__pycache__/
*.pyc
.pytest_cache/

# Raw data stays out of git (~1300 parquet files, one per game).
# The derived curated/ layer IS committed — Streamlit Cloud deploys from the repo.
data/raw/

.streamlit/secrets.toml
.env

# ingest run log
ingest.log

# scratch files from screenshots
*.tmp
